#include<stdio.h>
#include<string.h>
int main(){
    char para[]="last chapter you learnt how to define arrays";
    int num = strlen(para);
    printf("The number of characters in the string are: %d.\n ", num );
}